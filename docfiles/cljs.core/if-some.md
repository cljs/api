---
name: cljs.core/if-some
see also:
  - cljs.core/when-some
---

## Summary

## Details

If `test` is not nil, evaluates `then` with `x` bound to the value of `test`. If
not, yields `else`.

## Examples
