---
name: cljs.core/nthrest
see also:
  - cljs.core/drop
  - cljs.core/nthnext
  - cljs.core/nth
---

## Summary

## Details

Returns the `nth` rest of `coll`.

Returns `coll` when `n` is 0.

## Examples

## Signature
[coll n]
