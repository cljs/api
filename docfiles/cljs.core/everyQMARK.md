---
name: cljs.core/every?
see also:
  - cljs.core/some
  - cljs.core/not-any?
---

## Summary

## Details

Returns true if `(pred x)` is logical true for every `x` in `coll`, else false.

## Examples

## Signature
[pred coll]
