---
name: cljs.core/not-every?
see also:
  - cljs.core/every?
  - cljs.core/not-any?
  - cljs.core/some
---

## Summary

## Details

Returns false if `(pred x)` is logical true for every `x` in `coll`, else true.

## Examples

## Signature
[pred coll]
