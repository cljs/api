---
name: cljs.core/not-any?
see also:
  - cljs.core/every?
  - cljs.core/some
---

## Signature
[pred coll]


## Details

Returns false if `(pred x)` is logical true for any `x` in `coll`, else true.
