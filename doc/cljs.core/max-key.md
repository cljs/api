---
name: cljs.core/max-key
see also:
  - cljs.core/max
  - cljs.core/min-key
---

## Signature
[k x]
[k x y]
[k x y & more]


## Details

Returns the `x` for which `(k x)` is greatest.

`(k x)` should return a number.
