---
name: cljs.core/nthnext
related:
  - cljs.core/nth
  - cljs.core/drop
  - cljs.core/nthrest
---

## Signature
[coll n]


## Description

Returns the `n`th `next` of `coll`.

Returns `(seq coll)` when `n` is 0.
